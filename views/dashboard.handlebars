<!-- Dashboard handlebars -->
<div class="card-container">
    <div class="wrapper">

        <button class="btn outline"><a href="/new-post">Add Blog</a></button>
        <h1>{{user.name}}, checkout your posts...</h1>

        {{#each posts as |post|}}
        <div class="card-container">
            <div class="wrapper">
                 {{!-- title of blog  --}}
                {{!-- <p>{{post.title}}</h2> --}}
                {{!-- Food provider name --}}
                <h2 id="larger-text">{{post.name}}<br/></h2>
                {{!-- contact info --}}
                <h3>{{post.description}}<br/></h3>
                {{!-- food description --}}
                <p>{{post.content}}<br/></p>
            </div>
        </div>
        {{/each}}
    </div>
</div>